# this code requires python 3 and was tested with python 3.5
# run 'pip install -r requirements.txt' after going to this folder to install the following requirements

tensorflow >= 0.10
nltk
bs4
html5lib

# after installing nltk you must run python -> import nltk -> nltk.download() then hit b and then punkt